import styles from './styles.module.css'

export const Header = () => {
  return (
    <div className={styles.header}>
      <button>Timer App</button>
      <button>Figma Layout</button>
    </div>
  )
}
